# Description

Git mirror tool created using Go. It's mirror all the repos from github to gitlab

## Usage

Copy .env.example to .env and update the values. Place the .env file in the root of the project/binary.

Then run:

```bash
go-mirror start
```
